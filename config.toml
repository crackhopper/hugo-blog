baseURL = 'https://crackhopper.github.io'
languageCode = 'zh-cn'
title = 'crackhopper的技术博客'
theme = 'Stack'


[params]
  mainSections = ['posts']
  subtitle = 'C++, Graphics, Game, AI/ML'
  description = '爱游戏的技术宅'
  keywords = 'crackhopper, developer, graphics, game, c++'
  customCSS = ['css/custom.css']

  [params.author]
    name = 'crackhopper'
    email = 'crackhopper@gmail.com'

  [params.article]
    math = true

  [params.sidebar]
    subtitle = 'C++, Graphics, Game, AI/ML'  # 原来的副标题搬过来

    [params.sidebar.avatar]
      enabled = true
      local = true
      src = 'img/avatar.jpg'   # 对应 assets/img/avatar.jpg

  [params.widgets]
    # 针对首页和列表页的侧边栏
    [[params.widgets.homepage]]
      type = 'search'
    [[params.widgets.homepage]]
      type = 'categories'
      params = { limit = 10 }
    [[params.widgets.homepage]]
      type = 'tag-cloud'
      params = { limit = 10 }
    
    # 针对文章单页的侧边栏
    [[params.widgets.page]]
      type = 'toc'

[markup]
  [markup.tableOfContents]
    startLevel = 1 # 从 H2 (##) 开始生成 TOC
    endLevel = 4   # 到 H4 (####) 结束
    ordered = false # 使用无序列表
  [markup.highlight]
    style = 'github'
    lineNos = true
  [markup.goldmark]
    [markup.goldmark.renderer]
      # 允许在 Markdown 中使用 HTML 标签（用于图片宽度控制）
      unsafe = true
    [markup.goldmark.parser]
      [markup.goldmark.parser.attribute]
        block = true
        title = true
    [markup.goldmark.extensions]
      [markup.goldmark.extensions.passthrough]
        enable = true
        [markup.goldmark.extensions.passthrough.delimiters]
          block = [
            ['\[', '\]'],
            ['$$', '$$']
          ]
          inline = [
            ['\(', '\)']
          ]

[permalinks]
  posts = '/:year/:month/:day/:title/'
  page = '/:slug/'
  projects = '/projects/:slug/'

# Content Transform: 在解析 Markdown 之前转换 Obsidian 图片语法
# 注意：这需要 Hugo 0.100.0+ 版本
# 由于 Hugo 的 content transform 功能有限，我们使用 render hook 来处理
