# `src/hugo_blog/preview/admin_ui/package.json`

这个文件定义 React Admin/Docs 前端的 npm 脚本和依赖。

## 主要职责

- `npm run build`：执行 Vite production build。
- `npm run dev`：启动 Vite dev server。
- 声明 React、React DOM、TypeScript、Vite 和 React plugin。

## 修改注意

这里是本地工具前端，不是公开站点依赖。新增依赖前要确认它确实改善维护性，避免让初始化变慢。
